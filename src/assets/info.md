# In here I can only have static files like img, svg.
 ! Tive que aidiconar uma configuração para aceitar SVG como componente.
  # Criei o arquivo src/custom.d.ts
  # adicionei em tsconfig.json, includes: ['src/custom.d.ts']